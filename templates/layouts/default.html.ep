<!DOCTYPE html>
<html>
  <head>
    <title><%= $AltSimpleBoard::Data::Title %> - <%= acttitle() %></title>
    <%= stylesheet $AltSimpleBoard::Data::Themedir.session()->{theme}.'/css/style.css' %>
    <link rel="icon" type="image/ico" href="<%= url_for $AltSimpleBoard::Data::Themedir.session()->{theme}.'/img/favicon.png' %>" />
  </head>
  <body>
% if ( session()->{user} ) {
%= include 'parts/menu'
% }
%= include 'parts/error'
%= content
%= include 'parts/footerlinks'
  </body>
</html>
