<!DOCTYPE html>
<html>
  <head>
    <title>(<%= $newpostcount + $newmsgscount %>) <%= $configdata->{title} %></title>
  </head>
  <body>
    <!-- Angemeldet als "<%= session()->{user} || '<noone>' %>" !-->
<%= content %>    </body>
</html>

