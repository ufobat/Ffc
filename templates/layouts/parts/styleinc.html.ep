% if ( session->{backgroundcolor} ) {
    <style type="text/css">body { background-color: <%= session->{backgroundcolor} %> }</style>
% }
% if ( configdata->{customcss} ) {
    <%= stylesheet configdata->{customcss} %>
% }
