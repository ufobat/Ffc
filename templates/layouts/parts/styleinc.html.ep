% if ( $backgroundcolor or $fontsize ) {
    <style type="text/css">
      body {
%   if ( $backgroundcolor ) {
        background-color: <%= $backgroundcolor %>;
%   }
%   if ( $fontsize ) {
        font-size: <%= fontsize($fontsize) %>em;
%   }
      }
    </style>
% }
