<!DOCTYPE html>
<html>
  <head>
    <title><%= title %> - <%= AltSimpleBoard::Data::acttitle($act) %></title>
    <link rel="stylesheet" type="text/css" href="<%= url_for '/css/jquery.wysiwyg.css' %>" />
    <link rel="stylesheet" type="text/css" href="<%= url_for '/css/style.css' %>" />
    <script type="text/javascript" src="<%= url_for '/js/jquery.js' %>"></script>
    <script type="text/javascript" src="<%= url_for '/js/jquery.wysiwyg.js' %>"></script>
    <script type="text/javascript" src="<%= url_for '/js/wysiwyg.image.js' %>"></script>
    <script type="text/javascript" src="<%= url_for '/js/wysiwyg.link.js' %>"></script>
    <script type="text/javascript" src="<%= url_for '/js/wysiwyg.table.js' %>"></script>
    <script type="text/javascript">
        $(function() { $('#textinput').wysiwyg({
            autoGrow: true
        }); }); 
    </script>
  </head>
  <body>
<img src="<%= url_for '/img/banner.png' %>" alt="" class="banner" />
<img src="<%= url_for '/img/icons/' . $act . '.png' %>" alt="" class="actionicon" />
<div class="menu">
% if ( session()->{user} ) {
    <a href="<%= url_for 'frontpage', act => 'forum' %>" title="Nachrichten für alle angemeldeten Benutzer in der Art einer öffentlichen Diskussion"><span class="linktext linkforum<%= $act eq 'forum' ? ' active activeforum' : '' %>">öffentliches Forum</span></a>
    <span class="divider">|</span>
    <a href="<%= url_for 'frontpage', act => 'msgs' %>" title="Nachrichten, die nur du und dein jeweiliger Gesprächspartner lesen können"><span class="linktext linkmsgs<%= $act eq 'msgs' ? ' active activemsgs' : '' %>">private Nachrichten (0)</span></a>
    <span class="divider">|</span>
    <a href="<%= url_for 'frontpage', act => 'notes' %>" title="Kleine Notizen für dich selber und niemanden sonst"><span class="linktext linknotes<%= $act eq 'notes' ? ' active activenotes' : '' %>">eigene Notizen (0)</span></a>
    <span class="divider">|</span>
%= form_for search => ( method => 'POST' ) => begin
%= hidden_field 'act' => $act
%= text_field query => session()->{query}, session()->{query} ? (class => 'activesearch') : ()
%= submit_button 'Suchen', session()->{query} ? (class => 'activesearch') : ()
    <span class="divider">|</span>
    <a href="<%= url_for 'options' %>" title="Einstellungen für diese Plattform anpassen"><span class="linktext">Einstellungen</span></a>
    <span class="divider">|</span>
% end
    <a href="<%= url_for 'logout' %>" title="Vom System abmelden"><span class="linktext">abmelden</span> <span class="logout"> X</span></a>
% } else {
    <a href="<%= url_for 'registerform' %>" title="Für diese Plattform registrieren"><span class="linktext">Registrieren</span></a> 
% }
</div>
<h1 class="title"><%= title %> - <%= AltSimpleBoard::Data::acttitle($act) %></h1>
<%= content %>
<img src="<%= url_for '/img/bottom.png' %>" alt="" class="bottom" />
  </body>
</html>
