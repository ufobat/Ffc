% if ( $chatotherscnt ) {
    <div class="nodisplay" id="forumentries">
%    for my $u ( @$chat_users ) {
%      next if $u->[0] eq session->{user};
      <p class="dim"><%= $u->[0] %> (<%= $u->[1] %>)</p>
%    }
    </div>
% }

